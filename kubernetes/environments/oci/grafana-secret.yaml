# Value substituted by envsubst at deploy time; will be sealed in Phase 4.
apiVersion: v1
kind: Secret
metadata:
  name: grafana-secrets
  namespace: mankahi
type: Opaque
stringData:
  GF_SECURITY_ADMIN_PASSWORD: "${GRAFANA_ADMIN_PASSWORD}"
