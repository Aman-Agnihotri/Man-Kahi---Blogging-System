# Dev-only fake values (committed intentionally; NEVER real credentials).
# KEY=value format is deliberate: the repo secret-scan regex requires a colon.
MINIO_ACCESS_KEY=devminio
MINIO_SECRET_KEY=devkey7
