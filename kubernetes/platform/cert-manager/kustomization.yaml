apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cert-manager.yaml     # vendored verbatim from v1.21.0 release (see namespace-and-install.md)
  - cluster-issuer.yaml   # letsencrypt-staging + letsencrypt-prod ClusterIssuers
